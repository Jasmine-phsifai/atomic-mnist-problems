# AP-01-005: Broadcasting With a Named Feature Axis

Difficulty: 3/5
Runtime family: `numpy`

1. Read `statement.tex` or build `problem.tex`.
2. Implement only the `TODO` bodies in `starter.py`.
3. Run `python test_problem.py`.
4. Run `python benchmark.py` and interpret every printed/plotted diagnostic.

No reference solution is included. The external dataset is optional; tests use
deterministic synthetic fixtures.
